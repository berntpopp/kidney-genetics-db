name: Trivy Security Scan

on:
  push:
    branches:
      - main
      - dev/**
  pull_request:
    branches:
      - main
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  actions: read       # Required for private repos
  contents: read
  security-events: write

jobs:
  trivy-scan:
    name: Trivy Security Scan
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        target:
          - name: backend
            dockerfile: ./backend/Dockerfile
            context: ./backend
          - name: frontend
            dockerfile: ./frontend/Dockerfile
            context: ./frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker image for ${{ matrix.target.name }}
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.target.context }}
          file: ${{ matrix.target.dockerfile }}
          push: false
          load: true
          tags: kidney-genetics-${{ matrix.target.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner (Table Output)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: kidney-genetics-${{ matrix.target.name }}:${{ github.sha }}
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          trivyignores: '.trivyignore'

      - name: Run Trivy vulnerability scanner (SARIF Output)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: kidney-genetics-${{ matrix.target.name }}:${{ github.sha }}
          format: 'sarif'
          output: 'trivy-results-${{ matrix.target.name }}.sarif'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          trivyignores: '.trivyignore'

      - name: Upload Trivy scan results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        # Skip upload for pull requests from forks (lack permissions)
        if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
        continue-on-error: true
        with:
          sarif_file: 'trivy-results-${{ matrix.target.name }}.sarif'
          category: trivy-${{ matrix.target.name }}

  trivy-config-scan:
    name: Trivy Config Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Run Trivy misconfiguration scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'config'
          scan-ref: '.'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'
          skip-dirs: 'node_modules,venv,.venv,dist,build'

      - name: Run Trivy secret scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '0'
          scanners: 'secret'
          severity: 'CRITICAL,HIGH'
          skip-dirs: 'node_modules,venv,.venv,dist,build'

  trivy-postgres-scan:
    name: Trivy PostgreSQL Base Image Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Run Trivy on PostgreSQL base image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'postgres:17-alpine@sha256:dc17045ccfd343b49600570ea734b9c4991cf1c3f3302e67df51e3b402dd55c4'
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          trivyignores: '.trivyignore'
          # gosu (Go binary) only drops root->postgres once at container startup;
          # its Go-stdlib CVEs are not reachable in that usage and the upstream
          # postgres image ships a prebuilt binary we cannot patch. Skip it by path
          # (robust against the evolving Go CVE set); OS packages are still scanned.
          skip-files: 'usr/local/bin/gosu'

      - name: Run Trivy on PostgreSQL base image (SARIF)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'postgres:17-alpine@sha256:dc17045ccfd343b49600570ea734b9c4991cf1c3f3302e67df51e3b402dd55c4'
          format: 'sarif'
          output: 'trivy-results-postgres.sarif'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          trivyignores: '.trivyignore'
          # See note above: skip the unpatchable gosu Go binary by path.
          skip-files: 'usr/local/bin/gosu'

      - name: Upload Trivy PostgreSQL scan results
        uses: github/codeql-action/upload-sarif@v4
        # Skip upload for pull requests from forks (lack permissions)
        if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
        continue-on-error: true
        with:
          sarif_file: 'trivy-results-postgres.sarif'
          category: trivy-postgres
